/**
 * English locale — keys are the English text itself.
 * vue-i18n falls back to the key when no translation exists,
 * so this file only needs entries where the display differs from the key.
 */
const en: Record<string, string> = {}

export default en
